<% if errors.any? %>
	<div id="error_explanation">
		<h2><%= pluralize(errors.count, "error") %> prohibited this <%= model.class %> from being saved </h2>
		<p> there were problems on the following fields :</p>
		<ul>
			<% errors.full_messages.each do |msg| %>
				<li> <%= msg %> </li>
			<% end %>
			</ul>
			</div>
<% end %>